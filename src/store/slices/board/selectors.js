export const getBoard = (state) => state.boardReducer.board;
export const getWinner = (state) => state.boardReducer.winner;
export const getPlayer = (state) => state.boardReducer.player;
export const getTurnsCounter = (state) => state.boardReducer.turnsCounter;
